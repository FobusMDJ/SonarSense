# --- Preprocessing stage (in use now) ---
numpy>=1.26
opencv-python-headless>=4.9
scipy>=1.11
scikit-image>=0.22
PyYAML>=6.0
matplotlib>=3.8
tqdm>=4.66

# --- Denoiser training (Blind2Unblind) / VAE ---
torch>=2.2

# --- YOLO11 / YOLO26 detection stages (in use now) ---
# YOLO26 (src/detection/train_yolo26.py) needs a recent-enough ultralytics
# release to know the "yolo26*.pt" weight names -- confirmed present as of
# 8.4.x on PyPI; if `YOLO("yolo26n.pt")` raises a not-found error, just
# `pip install -U ultralytics`.
ultralytics>=8.4

# --- Later pipeline stages (classification) ---
# Uncomment as those modules are built.
# torchvision>=0.17

# --- Geolocation / geotagging engine (src/geolocation) ---
pyxtf>=1.3           # raw .xtf sonar log reader (src/geolocation/xtf_reader.py) -- UNVALIDATED, see that module's docstring
# pyproj>=3.6         # optional: exact geodesic offsets instead of the default flat-earth approximation (georeference.py)

# --- Backend API service (src/backend) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # required by FastAPI for UploadFile/form parsing
pydantic>=2.0
